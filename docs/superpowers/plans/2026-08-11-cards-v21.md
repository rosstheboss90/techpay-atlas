# Cards v2.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. All contracts, copy tables, and CSS values live in the **Amendment v2.1** section of `docs/superpowers/specs/2026-08-11-mobile-index-visual-design.md` — this plan only orders the work.

**Goal:** Apply amendment v2.1 — sentence facts, new questions, louder card presentation, M1 masthead, desktop-only TitleStrip.

**Branch:** `feat/cards-v21` (worktree `.worktrees/cards-v21`, real `npm ci` in `site/`).

### Task W1: teasers → sentences

- Modify `site/lib/teasers.ts` + `site/tests/teasers.test.ts` (TDD; tests to the amendment's fact table first, watch red).
- All five teasers return sentence `fact` + `context: ''`; `trendTeaser` gains the roleLabel arg and up/down verb; `titleTeaser`'s happy path becomes the "Job ads say…" sentence (it keeps its existing args).
- Keep: purity, fallback rows, `top3`/`topLabel`/`count`, SLOPE_N, metric guard, thin-count semantics.
- Whole-project tsc red until W2 — gate on the teasers test file only. Commit scoped.

### Task W2: page + headings + CSS

- `site/app/page.tsx`: masthead M1 (value line; provenance demoted); load `titles.json` best-effort in the Promise.all (memoized `get()`); tl-h card uses `titleTeaser`; h2h fact interpolates `meta.metros.length`; `{!narrow && <TitleStrip …/>}`; grid/h2h static sentences per table.
- Heading sweep (Task-6 pattern): five h2s + TrendsTeaser's h2 + SectionNav labels + e2e assertions + the page order-pin test → the amendment's question table.
- `site/app/globals.css`: card v2.1 presentation block (chevron circle replaces `.qcard-tap` text — QuestionSection markup change for the chevron span), full-width viz svg rule, masthead value-line/provenance styles.
- ALL gates green at end (vitest full, tsc, eslint). Commit scoped (page/css/QuestionSection/components touched by sweep/tests).

### Task W3 (controller): verify + ship

- One e2e run; visual pass both themes 390px (collapsed, expanded, metro-selected overflow path) + desktop spot-check (masthead + headings).
- BACKLOG ledger entry; final whole-branch review; push → PR → 3 checks → merge → live-bundle verify → ff main, remove worktree.
